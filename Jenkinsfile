pipeline {
       agent {
	     label { 
		  label "master"
		      }
	         }
	       stages {
		     
		    stage ("clone repo") {
		     steps {
			  sh "git clone https://github.com/adinathshelke/webapp.git"
		       }
		 }
	   }
}

